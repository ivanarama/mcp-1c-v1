# Миграция MCP сервера для поддержки настраиваемого транспорта

## Выполненные изменения

Ваш MCP сервер был обновлен для поддержки настраиваемого транспорта с минимальными изменениями:

### 1. Обновлены зависимости (`mcp/requirements.txt`)
Добавлен пакет `websockets` для расширенной поддержки WebSocket функциональности.

### 2. Добавлена конфигурация (`mcp/config.py`)
Добавлена новая настройка:
```python
TRANSPORT_TYPE = os.getenv("TRANSPORT_TYPE", "streamable-http")  # stdio, sse или streamable-http
```

### 3. Обновлен сервер (`mcp/mcp_server.py`)
- Импортирована переменная `TRANSPORT_TYPE`
- Изменен запуск сервера для использования настраиваемого транспорта

### 4. Обновлена Docker конфигурация
- Порт изменен с 9000 на 8001 для соответствия вашей конфигурации VSCode
- Добавлена переменная окружения для управления транспортом

## Как использовать

### Запуск с streamable-http (по умолчанию, совместимо с VSCode)
```bash
cd mcp
python3 mcp_server.py
```

### Запуск с SSE транспортом
```bash
cd mcp
TRANSPORT_TYPE=sse python3 mcp_server.py
```

### Запуск с STDIO транспортом
```bash
cd mcp
TRANSPORT_TYPE=stdio python3 mcp_server.py
```

## Установка зависимостей

Если у вас еще не установлены зависимости:

```bash
# Установка pip (если не установлен)
sudo apt install python3-pip

# Установка зависимостей
cd mcp
pip3 install -r requirements.txt
```

## Тестирование WebSocket

Включен тестовый скрипт `test_websocket.py` в корне проекта:

```bash
# Убедитесь что сервер запущен с WebSocket транспортом, затем:
python3 test_websocket.py

# Или с кастомными host/port:
python3 test_websocket.py localhost 9000
```

## Совместимость

- ✅ Полная обратная совместимость со старым SSE транспортом
- ✅ Все существующие API endpoints остаются работать
- ✅ MCP протокол работает одинаково на обоих транспортах
- ✅ Настройка транспорта через переменные окружения

## Преимущества WebSocket

- 🚀 Более эффективная двусторонняя связь
- 📱 Лучшая поддержка в современных клиентах
- ⚡ Меньшие накладные расходы по сравнению с SSE
- 🔄 Нативная поддержка в MCP библиотеках
